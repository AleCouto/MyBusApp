# MyBusApp agent guide

Read `.github/copilot-instructions.md` for the repository baseline. Then read
`.github/skills/minimalist-dev.md` and only the skill files relevant to the
requested work. Do not load every skill by default.

The source code and project configuration are the source of truth when they
conflict with a skill. Update the relevant skill when a deliberate architectural
change makes its guidance stale.
